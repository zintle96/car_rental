import { RegistationPipe } from './registation.pipe';

describe('RegistationPipe', () => {
  it('create an instance', () => {
    const pipe = new RegistationPipe();
    expect(pipe).toBeTruthy();
  });
});
